const apiRoutes = require("express").Router();

// GET route for api/workouts

// PUT route for api/workouts/ ID

// POST route for api/workouts/ creates new workout

// GET by Range route, api/workouts/range get workouts in range
